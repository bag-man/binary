{
  "name": "binary",
  "version": "1.0.0",
  "description": "Binary node app for all platforms",
  "scripts": {
    "dev": "tsc-watch --onSuccess 'node ./build/index.js'",
    "start": "node ./build/index.js",
    "clean": "rm -rf build/",
    "build": "npm run clean && webpack --config webpack.config.js --mode production && cp src/pkg.json build/package.json && cp -r assets/ build/",
    "package": "npm run build && pkg ./build --out-path build/bin/ --options no-warnings"
  },
  "author": "bag-man",
  "license": "GPL",
  "dependencies": {
    "axios": "^0.18.0",
    "bitcoinjs-lib": "^5.0.3"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/bitcoinjs-lib": "^4.0.1",
    "ts-loader": "^6.0.0",
    "tsc-watch": "^2.1.2",
    "typescript": "^3.4.5",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.2",
    "webpack-obfuscator": "^0.16.0"
  }
}
